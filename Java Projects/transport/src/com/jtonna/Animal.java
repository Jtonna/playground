package com.jtonna;

// the Animal interface is something that ALL animals will be able to use, and they all use the methods the same exact way.
public interface Animal
{
    void move();
    void eat(int i);
    String spreak();
}
